# Large run artifacts: use Git LFS so history stays small and checkouts stay consistent.
runs/**              filter=lfs diff=lfs merge=lfs -text
runs_latest/**       filter=lfs diff=lfs merge=lfs -text
